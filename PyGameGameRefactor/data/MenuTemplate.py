# class to be inherited by all menu states. it will have all of the draw and update code similar to each menu item, eg background, boarder,m etc..
# on state subclass have have ->
    # def update(...):
        #MenuTem,plate,update()
        #,...